<div class="container">
    <h1 class="page-header">Dashboard</h1>
    <h3>Welcome <%= req.user.name %></h3>
    
    <% keys.forEach((user)=>{ %>
        <p>
            <%= user %>
        </p>
        <a href='/admin/delete/?sessionid=<%=user%>'>Revoke session</a>
        <% }) %>
            

</div>
